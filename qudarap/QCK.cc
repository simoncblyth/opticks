#include "QCK.hh"

/*

see ~/np/tests/NPget_edgesTest.cc

template<typename T> T QCK::energy_lookup( const T BetaInverse, const T u) 
{
    int ibin = bis->pfindbin<T>(BetaInverse, 0u); 

    T lo, hi ; 
    bis->get_edges<double>(lo,hi,0u,ibin);

    assert( BetaInverse >= lo && BetaInverse < hi ); 



}

*/



// name=pending ; gcc $name.cc -I$HOME/np -std=c++11 -lstdc++ -o /tmp/$name && /tmp/$name

#include "NP.hh"
#include <vector>



int main(int argc, char** argv)
{
     // fake up some near and far distances 

     



    float tmin = 11 ; 

    unsigned num_sub = vv.size()/2  ; 
    unsigned pending = ( 0x1 << num_sub ) - 1 ;  

    // first pass 




    float envelope = 0.f ; 
 
    while(pending)
    {
        for(unsigned i=0 ; i < num_sub ; i++)
        {
            
            if( vals[i] < 
            pending &= ~(0x1 << isub) ;  // clear bit once found an exit for a sub
        }
        loop++; 
    }

    return 0 ; 
}
